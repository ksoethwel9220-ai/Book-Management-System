services:
  db:
    image: postgres:17.6-alpine        
    environment:
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: screatdb
      POSTGRES_DB: 
    ports:
      - "5432:5432"

  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db